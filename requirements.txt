# Grafana Reporting Service — Python dependencies
# Pin minor versions for reproducibility; allow patch updates.

# Configuration
python-dotenv>=1.0,<2.0

# HTTP / API clients
requests>=2.31,<3.0
urllib3>=2.0,<3.0

# Playwright (headless Chromium for Grafana screenshots)
playwright>=1.44,<2.0

# HTML templating
Jinja2>=3.1,<4.0
MarkupSafe>=2.1,<3.0

# PDF generation (HTML → PDF)
weasyprint>=61.0,<63.0

# Retry / resilience (used by requests adapter internally via urllib3)
# No extra package needed — urllib3.util.retry is bundled

# Optional: structured logging
# python-json-logger>=2.0,<3.0
